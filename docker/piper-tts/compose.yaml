services:
  piper-tts:
    build:
      context: .
    container_name: piper-tts
    restart: unless-stopped
    ports:
      - "127.0.0.1:${PIPER_TTS_PORT:-9010}:9010"
    volumes:
      - piper-tts-data:/var/lib/piper-tts
    env_file:
      - .env

volumes:
  piper-tts-data:
