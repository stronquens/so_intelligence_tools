## ADDED Requirements

### Requirement: Highlights Runtime Readiness

The public README SHALL show whether each highlighted capability is prepared for local/on-prem execution, API-backed execution, both, or planned runtime support.

#### Scenario: Reader compares highlighted capabilities

- **WHEN** a reader views the README Highlights table
- **THEN** each row includes a concise runtime readiness value in addition to the existing feature status
