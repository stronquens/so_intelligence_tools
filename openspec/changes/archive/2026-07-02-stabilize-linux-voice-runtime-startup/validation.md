# Validation

Date: 2026-07-02.
Host: local Linux workstation.

## Commands Run

```bash
poetry run pytest tests/test_user_services.py tests/test_local_tts_client.py tests/test_faster_whisper_http.py
poetry run python -m py_compile src/so_intelligence_tools/infrastructure/user_services.py src/so_intelligence_tools/cli/main.py
poetry run openspec validate stabilize-linux-voice-runtime-startup --strict
so-ai install-linux-voice-runtimes-service
so-ai install-push-to-talk-dictation-service
systemctl --user status so-intelligence-tools-voice-runtimes.service --no-pager -l
systemctl --user cat so-intelligence-tools-push-to-talk-dictation.service
systemctl --user is-enabled so-intelligence-tools-voice-runtimes.service
systemctl --user is-active so-intelligence-tools-voice-runtimes.service
systemctl --user is-active so-intelligence-tools-push-to-talk-dictation.service
so-ai check-push-to-talk-dictation-runtime
so-ai status-piper-tts-server
```

## Results

- Unit tests passed: 19 passed.
- Python compile checks passed.
- OpenSpec validation passed.
- `so-intelligence-tools-voice-runtimes.service` is installed, enabled, and active.
- Re-running `so-ai install-linux-voice-runtimes-service` restarts the oneshot service, so repair/install commands force a fresh runtime ensure even when the unit was already active.
- The voice runtime service successfully ensured:
  - `docker/whisper-server/.env`
  - `docker/piper-tts/.env`
- The installed voice runtime service includes `Restart=on-failure` and `RestartSec=10` so it retries if Docker is still starting during login.
- The dictation user service now contains:
  - `After=graphical-session.target so-intelligence-tools-voice-runtimes.service`
  - `Wants=graphical-session.target so-intelligence-tools-voice-runtimes.service`
- `so-ai check-push-to-talk-dictation-runtime` reports ready.
- `so-ai status-piper-tts-server` reports ready.

## Notes

- The user-level runtime service sets `DOCKER_CONTEXT=default` so login startup does not depend on Docker Desktop being selected or running.
- `WHISPER_API_KEY=` is explicitly included in the Whisper environment to avoid autogenerated API-key auth for the localhost-only development server.
