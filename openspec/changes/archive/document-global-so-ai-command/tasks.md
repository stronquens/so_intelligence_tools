## 1. Documentation

- [x] 1.1 Document the persistent `so-ai` command in the Linux getting-started guide.
- [x] 1.2 Update Piper/Codex voice docs to use `so-ai` for per-window controls.
- [x] 1.3 Clarify that `listen-codex-visible-events --detail ...` is a listener for piped JSONL, not a setting command.

## 2. Validation

- [x] 2.1 Validate the OpenSpec change.
- [x] 2.2 Verify the documented command resolves locally.
