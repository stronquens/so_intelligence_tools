## MODIFIED Requirements

### Requirement: Desktop translation UI reachability
The realtime translation desktop UI SHALL be reachable from the new overlay launcher.

#### Scenario: User opens translation from overlay
- **WHEN** the user selects the overlay audio translation action
- **THEN** the app SHALL open or focus the new translator view in a separate Electron window.
